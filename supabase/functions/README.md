# Edge Functions

This folder contains representative Supabase Edge Functions used to support backend automation and operational workflows.

Current example:
- `payroll_xml_import`: imports payroll XML files, deduplicates input files, normalizes punch data, rolls up time entries, and flags unmatched records for review.
